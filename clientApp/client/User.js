/**
 *	User of a chat client application.
 *	- authenticates with Google accounts
 *	- has list of contacts
 *	- participates in chat windows
 */
 
var User = Backbone.Model.extend({

	initialize : function() { 
		// get user info from cache
	},

	authenticate : function() { 
		// run authentication code here
		return true;
	}

});
